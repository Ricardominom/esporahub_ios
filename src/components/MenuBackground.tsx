import React, { useEffect, useRef } from 'react';

const MenuBackground: React.FC = () => {
  // Background animation completely removed for iPad interface
  return null;
};

export default MenuBackground;